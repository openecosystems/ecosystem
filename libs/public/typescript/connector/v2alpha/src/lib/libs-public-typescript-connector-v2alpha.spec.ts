import { libsPublicTypescriptConnectorV2alpha } from './libs-public-typescript-connector-v2alpha';

describe('libsPublicTypescriptConnectorV2alpha', () => {
  it('should work', () => {
    expect(libsPublicTypescriptConnectorV2alpha()).toEqual(
      'libs-public-typescript-connector-v2alpha'
    );
  });
});
