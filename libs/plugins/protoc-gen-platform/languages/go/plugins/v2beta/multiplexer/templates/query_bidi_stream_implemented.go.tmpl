{{ $s := parentService . }}{{ $goPath := goPath .File }}{{ $e := parentEntity . }}func (s *{{ $s.Name }}Handler) {{ .Name }}(stream {{ package .File }}.{{ $s.Name }}_{{ .Name }}Server) error {

return nil
}
